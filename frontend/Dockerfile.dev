FROM node:22-alpine

WORKDIR /app

# npm ci respeita o lockfile e a pasta node_modules fica em volume no compose.
COPY package.json package-lock.json ./
RUN npm ci

COPY . .

ENV HOST=0.0.0.0
EXPOSE 3000

CMD ["npm", "start"]
